#config service
server:
  port: 8081
  servlet:
    context-path: /user
spring:
  application:
    name: dota-user-service
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
management:
  endpoints:
    web:
      exposure:
        include=*:




